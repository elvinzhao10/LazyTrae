diff --git a/duration_parser/parse_duration.py b/duration_parser/parse_duration.py
index bef79d0..bc3b41f 100644
--- a/duration_parser/parse_duration.py
+++ b/duration_parser/parse_duration.py
@@ -1,7 +1,49 @@
+import re
+
+
 def parse_duration(value: str) -> int:
-    amount = int(value[:-1])
-    return amount * {
+    if not isinstance(value, str):
+        raise TypeError("expected a string")
+
+    stripped = value.strip()
+    if not stripped:
+        raise ValueError("empty string")
+
+    # Detect internal whitespace
+    if re.search(r"\s", stripped):
+        raise ValueError("internal whitespace not allowed")
+
+    # Split into numeric part and unit suffix
+    if stripped.endswith("ms"):
+        unit = "ms"
+        num_part = stripped[:-2]
+    else:
+        unit = stripped[-1]
+        num_part = stripped[:-1]
+
+    if not num_part:
+        raise ValueError("missing numeric value")
+
+    # Reject decimal notation
+    if "." in num_part:
+        raise ValueError("decimal values not allowed")
+
+    try:
+        amount = int(num_part)
+    except ValueError:
+        raise ValueError(f"invalid numeric value: {num_part}")
+
+    if amount < 0:
+        raise ValueError("negative values not allowed")
+
+    multipliers = {
+        "ms": 1,
         "s": 1_000,
         "m": 60_000,
         "h": 3_600_000,
-    }[value[-1]]
\ No newline at end of file
+    }
+
+    if unit not in multipliers:
+        raise ValueError(f"unknown unit: {unit}")
+
+    return amount * multipliers[unit]
\ No newline at end of file
