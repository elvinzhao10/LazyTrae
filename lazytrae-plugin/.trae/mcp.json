{
  "mcpServers": {
    "lazytrae": {
      "command": "lazytrae",
      "args": ["mcp"],
      "description": "LazyTrae state, evidence, handoff, and local context MCP server — exposes 15 tools including heuristic symbol/reference/docs/dependency helpers"
    },
    "grep_app": {
      "url": "https://mcp.grep.app",
      "required": false,
      "description": "Remote code search from grep.app"
    },
    "context7": {
      "url": "https://mcp.context7.com/mcp",
      "required": false,
      "description": "Documentation lookup for open-source libraries"
    },
    "filesystem": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-filesystem@2026.7.10", "."],
      "required": false,
      "description": "Filesystem access MCP server — optional template pinned to a reviewed version"
    },
    "git": {
      "required": false,
      "disabled": true,
      "description": "Git operations MCP server — disabled manual option. The former package name is no longer published; install and configure a reviewed Git MCP server explicitly before enabling it."
    },
    "playwright": {
      "command": "npx",
      "args": ["-y", "@playwright/mcp@0.0.78"],
      "required": false,
      "description": "Browser automation MCP server via Playwright — optional template pinned to a reviewed version"
    },
    "context7_docs": {
      "url": "https://mcp.context7.com/mcp",
      "required": false,
      "description": "Documentation search MCP server — optional template (alias for context7)"
    },
    "ast_grep": {
      "required": false,
      "disabled": true,
      "description": "Structural code search via ast-grep — disabled manual option. The former package name is no longer published; install and configure a reviewed MCP server explicitly before enabling it."
    },
    "lsp": {
      "required": false,
      "disabled": true,
      "description": "Language Server Protocol diagnostics — disabled manual option. Review and configure a local LSP MCP server explicitly before enabling it; Trae has no built-in LSP."
    },
    "codegraph": {
      "required": false,
      "disabled": true,
      "description": "Code graph analysis — disabled manual option. LazyTrae compensates with SearchCodebase; review and configure a suitable CodeGraph MCP server explicitly before enabling it."
    }
  },
  "lazytrae": {
    "description": "LazyTrae MCP configuration template for v0.15 alpha. The 'lazytrae' server runs through the installed CLI and provides 15 state, evidence, handoff, and local context tools. Optional servers provide additional capabilities and degrade gracefully when not configured.",
    "note": "Required: false servers are optional and will not cause errors if unavailable. Disable unwanted servers by setting 'disabled': true."
  }
}
