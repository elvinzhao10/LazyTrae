{
  "mcpServers": {
    "lazytrae": {
      "command": "node",
      "args": ["packages/mcp/src/index.js"],
      "cwd": ".",
      "description": "LazyTrae state query and mutation MCP server — exposes 9 tools for boulder state, evidence recording, parity checking, and handoff generation"
    },
    "grep_app": {
      "url": "https://mcp.grep.app",
      "required": false,
      "description": "Remote code search from grep.app — mirrors LazyCodex grep_app MCP"
    },
    "context7": {
      "url": "https://mcp.context7.com/mcp",
      "required": false,
      "description": "Documentation lookup for open-source libraries — mirrors LazyCodex context7 MCP"
    },
    "filesystem": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-filesystem", "."],
      "required": false,
      "description": "Filesystem access MCP server — optional template"
    },
    "git": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-git"],
      "required": false,
      "description": "Git operations MCP server — optional template"
    },
    "playwright": {
      "command": "npx",
      "args": ["-y", "@playwright/mcp@latest"],
      "required": false,
      "description": "Browser automation MCP server via Playwright — optional template"
    },
    "context7_docs": {
      "url": "https://mcp.context7.com/mcp",
      "required": false,
      "description": "Documentation search MCP server — optional template (alias for context7)"
    },
    "ast_grep": {
      "command": "npx",
      "args": ["-y", "@ast-grep/mcp"],
      "required": false,
      "description": "Structural code search via ast-grep — optional template"
    },
    "lsp": {
      "command": "npx",
      "args": ["-y", "lsp-mcp"],
      "required": false,
      "description": "Language Server Protocol diagnostics — optional template"
    }
  },
  "lazytrae": {
    "description": "LazyTrae MCP configuration for v0.8. The 'lazytrae' server provides 9 tools to query and mutate LazyTrae state. Optional servers provide additional capabilities and degrade gracefully when not configured."
  }
}
